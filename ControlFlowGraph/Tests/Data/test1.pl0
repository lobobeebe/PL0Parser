const y = 5, z = 0;
var x, sum;

procedure addition;
    var a, b;

    procedure hello;
        begin
            x := 20;
            sum := (sum + x)
        end;

    begin
        a := x;
        b := y;
        if x <= 5 then print x;
        read x;
        sanitize x;
        print x;
        sum := (a + b)
    end;

begin
    x := 2;
    call addition;
    sum := (sum + 1);
    read sum;
    if sum > 40 then begin
        while x # 20 do begin
            x := y;
            sum := (y - 20) + (5 * 2)
        end;
        sum := 0
    end;
    call hello
end.
